# 表单模态框最佳实践 - 经验库

## 核心经验

### 1. 统一的表单验证模式
**模式**：前端验证 + 后端验证
- 前端验证：即时反馈，提升用户体验
- 后端验证：确保数据安全，防止恶意数据

### 2. 模态框状态管理
**标准状态变量**：
```javascript
const [showModal, setShowModal] = useState(false);
const [formData, setFormData] = useState({...});
const [error, setError] = useState('');
const [success, setSuccess] = useState('');
```

### 3. 必填字段标记
**视觉标准**：使用红色星号标记必填字段
```jsx
<label className="block text-sm font-medium text-gray-700 mb-1">
  标题 <span className="text-red-500">*</span>
</label>
```

### 4. 表单验证流程
**最佳实践**：
1. 验证必填字段是否已填写
2. 验证数据格式（文件类型、大小等）
3. 验证业务规则
4. 显示错误信息
5. 验证通过后提交

### 5. 成功反馈机制
**用户体验**：
- 显示成功消息
- 延迟关闭模态框（1.5秒）
- 刷新列表数据

### 6. 文件上传最佳实践
**关键要点**：
1. 设置`bodyParser: false`在Next.js API路由
2. 使用`formidable`库解析文件
3. 验证文件类型和大小
4. 生成唯一文件名避免冲突
5. 清理临时文件

### 7. JWT令牌验证一致性
**核心原则**：
- 所有API使用相同的密钥验证方式
- 优先使用环境变量`JWT_SECRET`
- 所有验证失败返回相同的错误格式

## 可复用代码模式

### 模式1：模态框表单组件
```jsx
export default function ModalForm({ onSave, onClose, item = null }) {
  const [formData, setFormData] = useState(item || {});
  const [error, setError] = useState('');
  const [success, setSuccess] = useState('');

  const handleSubmit = async (e) => {
    e.preventDefault();
    setError('');
    setSuccess('');
    
    // 验证逻辑
    if (!formData.title) {
      setError('标题为必填项');
      return;
    }
    
    // 提交逻辑
  };

  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
      <div className="bg-white rounded-lg shadow-xl p-6 w-full max-w-2xl">
        {/* 表单内容 */}
      </div>
    </div>
  );
}
```

### 模式2：API路由配置（文件上传）
```javascript
export const config = {
  api: {
    bodyParser: false, // 禁用内置解析器
  },
};

const JWT_SECRET = process.env.JWT_SECRET || 'your-secret-key';

function getUserIdFromToken(req) {
  const token = req.headers.authorization?.replace('Bearer ', '');
  if (!token) return null;
  try {
    const decoded = jwt.verify(token, JWT_SECRET);
    return decoded.id;
  } catch (error) {
    return null;
  }
}
```

### 模式3：表单验证与提交函数
```javascript
const handleSubmit = async (e) => {
  e.preventDefault();
  try {
    setError('');
    setSuccess('');
    
    // 1. 验证必填字段
    if (!formData.title) {
      setError('标题为必填项');
      return;
    }
    
    // 2. 验证文件（如需要）
    if (file && !allowedTypes.includes(file.type)) {
      setError('不支持的文件类型');
      return;
    }
    
    // 3. 提交数据
    const response = await api.create(formData);
    
    // 4. 成功处理
    if (response.status === 'success') {
      setSuccess('保存成功');
      setTimeout(() => {
        onSave(response.data);
        onClose();
      }, 1500);
    }
  } catch (error) {
    setError(error.message || '保存失败');
  }
};
```

## 设计原则

1. **一致性**：相同类型的表单使用相同的UI和交互模式
2. **清晰性**：必填字段明确标记，错误消息清晰易懂
3. **反馈性**：操作结果及时反馈给用户
4. **安全性**：前后端双重验证，确保数据安全
5. **可维护性**：代码结构清晰，易于修改和扩展

## 应用场景

- 视频上传/编辑表单
- 照片上传/编辑表单
- 里程碑添加/编辑表单
- 日记添加/编辑表单
- 事件添加/编辑表单
- 成长记录添加/编辑表单
